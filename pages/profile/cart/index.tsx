import { Layout } from '@components/common';

const Cart = () => {
    return (
        <Layout footer searchBar selected="cart">
            <div>this is Cart page</div>;
        </Layout>
    );
};

export default Cart;
